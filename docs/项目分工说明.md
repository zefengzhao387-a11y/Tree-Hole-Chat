# 解忧树洞 · 小组分工

| 姓名 | 角色 | 负责内容 |
|------|------|----------|
| 赵泽峰 | 组长 | 见下表 |
| 那亮 | 后端 | 见下表 |
| 郝昡 | 前端 | 见下表 |

---

## 赵泽峰

**负责：** 工程骨架、RAG 实现、部署联调、接口文档、答辩统稿

| 工作 | 目录 / 范围 |
|------|-------------|
| 项目骨架 | `main.py`、`database.py`、`config.py`、`models/` 表结构定稿、`.env.example` |
| RAG 全部 | `backend/app/rag/`（Embedding、分块、Chroma、检索、Prompt、Chain） |
| 部署与文档 | `docker-compose.yml`、`README.md`、`docs/api.md` |
| 联调与合并 | 前后端打通、Code Review、合并主分支 |

**不做：** 具体业务 CRUD 代码、前端页面

---

## 那亮

**负责：** 后端业务详细实现（不含 RAG）

| 工作 | 目录 / 范围 |
|------|-------------|
| 认证 | `core/security.py`、`deps/auth.py`、`routers/auth.py` |
| 日记 | `routers/diary.py`、`services/diary_service.py` |
| 情感分析 | `services/emotion_analyzer.py`、`routers/analysis.py` |
| 树洞对话 | `routers/chat.py`、`services/chat_service.py` |
| 数据契约 | `schemas/` |
| 测试 | `backend/tests/` |

**调用组长的 RAG 接口：** `index_diary`、`delete_diary_vectors`、`analyze_emotion`、`stream_chat_reply`

**不做：** 修改 `rag/` 目录、任何前端文件

---

## 郝昡

**负责：** 前端全部实现

| 工作 | 目录 / 范围 |
|------|-------------|
| 全部页面 | `Landing`、`Auth`、`DiaryList`、`DiaryEdit`、`TreeHoleChat`、`EmotionTrend`、`Settings` |
| 组件与布局 | `components/`、`layouts/`、`NavBar` |
| 路由与状态 | `router/`、`stores/` |
| 接口对接 | `api/`（按 `docs/api.md`） |
| 主题样式 | `App.vue`、`styles/theme.css`、`public/` |

**不做：** 任何 `backend/` 文件

---

## 协作约定

- 那亮与郝昡之间：只按 `docs/api.md` 对接，接口变更先改文档再通知对方
- 那亮与组长之间：只调用 RAG 对外函数，不改 `rag/` 内部
- 代码合并：那亮、郝昡提 PR，组长 Review 后合入

---

## 按功能分工

| 功能 | 组长 | 那亮 | 郝昡 |
|------|:----:|:----:|:----:|
| 注册 / 登录 | 定 API | 实现 | 页面 |
| 日记增删改查 | — | 实现 | 页面 |
| 日记向量入库 | RAG | 调用 | — |
| 情感分析 | Prompt / Chain | API | 标签 / 趋势图 |
| 树洞对话 | 检索 / Chain | SSE API | 聊天界面 |
| Docker 部署 | 负责 | 保证测试通过 | 保证构建通过 |

---

*接口细节见 `docs/api.md`*
